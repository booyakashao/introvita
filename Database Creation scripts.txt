DB Credentials
username: introvita_user
password: introvita_pwd_5321


CREATE TABLE "user"
(
  id serial NOT NULL,
  "firstName" character varying(100),
  "middleName" character varying(50),
  "lastName" character varying(100),
  "addressLine1" character varying(300),
  "addressLine2" character varying(300),
  "phoneNumber" character varying(20),
  "emailAddress" character varying(200),
  city character varying(200),
  state bigint,
  zipcode integer,
  "paymentId" character varying(500),
  company character varying(100),
  department character varying(200),
  "positionAtCompany" character varying(150),
  "authorityId" bigint,
  CONSTRAINT pk_user_id PRIMARY KEY (id)
)
WITH (
  OIDS=FALSE
);
ALTER TABLE "user"
  OWNER TO introvita_user;

CREATE TABLE state
(
  id serial NOT NULL,
  name character varying(2),
  CONSTRAINT pk_state_id PRIMARY KEY (id)
)
WITH (
  OIDS=FALSE
);
ALTER TABLE state
  OWNER TO introvita_user;

CREATE TABLE authority
(
  id integer NOT NULL DEFAULT nextval('"Authority_id_seq"'::regclass),
  name character varying(150) NOT NULL,
  CONSTRAINT pk_authority_id PRIMARY KEY (id, name)
)
WITH (
  OIDS=FALSE
);
ALTER TABLE authority
  OWNER TO introvita_user;

CREATE TABLE candidate_attributes
(
  id serial NOT NULL,
  "passionsAndGoals" character varying(500),
  "workProjectDone" text,
  skills text,
  "userId" bigint,
  CONSTRAINT pk_candidate_attributes PRIMARY KEY (id)
)
WITH (
  OIDS=FALSE
);
ALTER TABLE candidate_attributes
  OWNER TO introvita_user;


CREATE TABLE "job_post"
(
  id serial NOT NULL,
  title character varying(100),
  company character varying(100),
  about text,
  summary text,
  qualifications text,
  responsibilites text,
  "employerId" bigint,
  salary character varying(150),
  "limit" integer,
  applied integer,
  enabled boolean DEFAULT false,
  questions_id bigint,
  CONSTRAINT pk_job_post PRIMARY KEY (id)
)
WITH (
  OIDS=FALSE
);
ALTER TABLE "job_post"
  OWNER TO introvita_user;

CREATE TABLE questions
(
  id serial NOT NULL,
  CONSTRAINT pk_questions PRIMARY KEY (id)
)
WITH (
  OIDS=FALSE
);
ALTER TABLE questions
  OWNER TO introvita_user;

CREATE TABLE question
(
  id serial NOT NULL,
  questions_id bigint,
  question character varying(500),
  CONSTRAINT pk_question PRIMARY KEY (id)
)
WITH (
  OIDS=FALSE
);
ALTER TABLE question
  OWNER TO introvita_user;

CREATE TABLE answer
(
  id serial NOT NULL,
  question_id bigint,
  candidate_id bigint,
  answer character varying(250),
  CONSTRAINT pk_answer PRIMARY KEY (id)
)
WITH (
  OIDS=FALSE
);
ALTER TABLE answer
  OWNER TO introvita_user;

CREATE TABLE application_to_job
(
  id serial NOT NULL,
  candidate_id bigint,
  job_post_id bigint,
  applied boolean,
  CONSTRAINT pk_application_to_job PRIMARY KEY (id)
)
WITH (
  OIDS=FALSE
);
ALTER TABLE application_to_job
  OWNER TO introvita_user;


